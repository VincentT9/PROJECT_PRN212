<Window x:Class="Wpf_SchoolMedicalManangementSystem.ParentalConsentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_SchoolMedicalManangementSystem"
        mc:Ignorable="d"
        Title="ParentalConsentView" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Chọn chiến dịch -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Chiến dịch:" Width="100" VerticalAlignment="Center"/>
            <ComboBox x:Name="CampaignComboBox" Width="350" DisplayMemberPath="Name"/>
        </StackPanel>

        <!-- Chọn học sinh -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" Grid.Row="1">
            <TextBlock Text="Học sinh:" Width="100" VerticalAlignment="Center"/>
            <ComboBox x:Name="StudentComboBox" Width="350" DisplayMemberPath="FullName"/>
        </StackPanel>

        <!-- Ghi chú nếu từ chối -->
        <TextBox x:Name="DeclineReasonTextBox"
                 Grid.Row="2"
                 Height="100"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 Text="Lý do từ chối (nếu có)"/>

        <!-- Nút hành động -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Đồng ý" Width="100" Margin="0,0,10,0" Click="ApproveButton_Click"/>
            <Button Content="Từ chối" Width="100" Click="DeclineButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
