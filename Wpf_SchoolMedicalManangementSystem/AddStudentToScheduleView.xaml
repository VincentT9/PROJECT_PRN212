<Window x:Class="Wpf_SchoolMedicalManangementSystem.AddStudentToScheduleView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_SchoolMedicalManangementSystem"
        mc:Ignorable="d"
        Title="AddStudentToScheduleView" Height="600
        " Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Chọn lịch trình:" VerticalAlignment="Center"/>
            <ComboBox x:Name="ScheduleComboBox" Width="300" Margin="10,0"
                      DisplayMemberPath="ScheduledDate"
                      SelectionChanged="ScheduleComboBox_SelectionChanged"/>
        </StackPanel>

        <GroupBox Header="Học sinh trong lịch trình" Grid.Row="1">
            <ListView x:Name="StudentListView" DisplayMemberPath="FullName"/>
        </GroupBox>
        
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,10,0,0">
            <TextBlock Text="Thêm học sinh:" VerticalAlignment="Center"/>
            <ComboBox x:Name="StudentComboBox" Width="300" Margin="10,0"
                      DisplayMemberPath="FullName"/>
            <Button Content="Thêm học sinh" Click="AddStudentButton_Click" Margin="10,0"/>
        </StackPanel>
    </Grid>
</Window>
