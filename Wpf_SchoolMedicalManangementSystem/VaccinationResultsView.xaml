<Window x:Class="Wpf_SchoolMedicalManangementSystem.VaccinationResultsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_SchoolMedicalManangementSystem"
        mc:Ignorable="d"
        Title="VaccinationResultsView" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <!-- Danh sách học sinh -->
            <ColumnDefinition Width="3*"/>
            <!-- Danh sách kết quả -->
            <ColumnDefinition Width="3*"/>
            <!-- Chi tiết -->
        </Grid.ColumnDefinitions>

        <!-- Danh sách học sinh -->
        <GroupBox Header="Học sinh" Margin="5">
            <ListBox x:Name="StudentListBox"
                     DisplayMemberPath="FullName"
                     SelectionChanged="StudentListBox_SelectionChanged"/>
        </GroupBox>

        <!-- Danh sách kết quả tiêm -->
        <GroupBox Header="Kết quả tiêm chủng" Grid.Column="1" Margin="5">
            <ListBox x:Name="VaccinationResultListBox"
                     DisplayMemberPath="DosageGiven"
                     SelectionChanged="VaccinationResultListBox_SelectionChanged"/>
        </GroupBox>

        <!-- Chi tiết kết quả -->
        <GroupBox Header="Chi tiết" Grid.Column="2" Margin="5">
            <StackPanel Margin="10">
                <TextBlock Text="Liều lượng:" FontWeight="Bold"/>
                <TextBlock x:Name="DosageTextBlock" Margin="0,0,0,10"/>

                <TextBlock Text="Tác dụng phụ:" FontWeight="Bold"/>
                <TextBlock x:Name="SideEffectsTextBlock" Margin="0,0,0,10"/>

                <TextBlock Text="Ghi chú:" FontWeight="Bold"/>
                <TextBlock x:Name="NotesTextBlock" Margin="0,0,0,10"/>

                <TextBlock Text="Ngày tiêm:" FontWeight="Bold"/>
                <TextBlock x:Name="VaccinationDateTextBlock" Margin="0,0,0,10"/>

                <TextBlock Text="Ngày tạo:" FontWeight="Bold"/>
                <TextBlock x:Name="CreateAtTextBlock" Margin="0,0,0,10"/>

                <TextBlock Text="Cập nhật lần cuối:" FontWeight="Bold"/>
                <TextBlock x:Name="UpdateAtTextBlock"/>
            </StackPanel>
        </GroupBox>
    </Grid>
</Window>
