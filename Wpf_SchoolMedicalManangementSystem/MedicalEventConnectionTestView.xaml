<Page x:Class="Wpf_SchoolMedicalManangementSystem.MedicalEventConnectionTestView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Test Kết nối Database"
      Height="600"
      Width="800">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="🔍 KIỂM TRA KẾT NỐI CƠ SỞ DỮ LIỆU"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"
                   Foreground="#2E86AB"/>

        <!-- Messages List -->
        <Border Grid.Row="1"
                Background="#F8F9FA"
                CornerRadius="5"
                Padding="15"
                BorderBrush="#DDD"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding TestMessages}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}"
                                       Margin="0,2"
                                       FontSize="14"
                                       FontFamily="Consolas"
                                       TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Buttons -->
        <StackPanel Grid.Row="2"
                Orientation="Horizontal"
                HorizontalAlignment="Center"
                Margin="0,20,0,0">
            <Button x:Name="btnRetry"
                    Content="🔄 Thử lại"
                    Width="120"
                    Height="35"
                    Margin="0,0,10,0"
                    Background="#FFC107"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BtnRetry_Click"/>
            <Button x:Name="btnBack"
                    Content="🏠 Quay lại"
                    Width="120"
                    Height="35"
                    Background="#6C757D"
                    Foreground="White"
                    BorderThickness="0"
                    Click="BtnBack_Click"/>
        </StackPanel>
    </Grid>
</Page>
